# Bitácora de salud

Append-only: una fila por pasada de `/salud`, la más reciente abajo. Registra
qué corrió, qué encontró y qué se hizo — es lo que permite rotar la mutación,
no re-diagnosticar mutantes equivalentes conocidos y vigilar si el fuzzing
sigue encontrando corpus nuevo.

| Fecha | Qué corrió | Hallazgos | Acción |
|---|---|---|---|
| 2026-07-10 | Estreno de la capa. Fuzzing: 6 dianas nuevas (`fuzz_test.go`), humo 10s/diana + 60s en wrap. Race: `-count=1` (humo). govulncheck v1.1.4. Mutación: `diff.go` (piloto de la skill /mutacion, 95 mutantes). | **2 bugs reales en 🔒 S22** (misma familia: `curW==0`/`curW>0` como centinela de vacío borra una palabra de anchura 0 en `wrapParagraph` y pega un cluster ancho al ZWJ huérfano en `splitWide`; corpus `FuzzWrapText/*` committeado). **3 CVEs stdlib alcanzables**: GO-2026-5856 (crypto/tls, fix go1.26.5), GO-2026-5039 (net/textproto, fix go1.26.4), GO-2026-5037 (crypto/x509, fix go1.26.4) — toolchain local go1.26.3. Mutación diff.go: 90/95 KILLED; LIVED: 216/239 equivalentes de borde, 146 (desempate LCS) y 361 (`rangeStr` no aseverado en render) = huecos de suite pendientes. Observación menor sin arreglar: `wrapSpans` (markdown.go:577) comparte el patrón centinela pero solo pierde el espacio reinsertado tras un token de anchura 0 (cosmético, render visual). | Bugs S22 arreglados con casos nombrados en `TestWrapText` + corpus de regresión. CVEs: **pendiente subir toolchain a go1.26.5** (decisión del usuario). Huecos de diff.go: pendientes de test (candidata a próxima sesión). Próxima rotación de mutación: NO diff.go (p. ej. sse.go/text.go). |
| 2026-07-11 | Remediación de CVEs: `toolchain go1.26.5` en go.mod (única fuente de verdad — cubre local, CI y releases vía `go-version-file`). govulncheck re-ejecutado. Suites `-race -shuffle=on` (raíz + runtime) y vmwasm en verde con 1.26.5. | govulncheck: **0 vulnerabilidades alcanzables** (queda 1 en paquete importado que el código no llama). **Hallazgo nuevo**: `TestSearchGrepEarlyStopNoLeak` es flaky (~1-2/10 con `-count=10`) — tras 2 s de deadline quedan ~7 goroutines sobre la base tras abandonar el iterador de `grep`; reproduce igual con 1.26.3 (preexistente, no es del toolchain). Puede ser fuga real intermitente del pool en early-stop, no solo test frágil. | CVEs cerrados. La flake/fuga de grep: **candidata a investigación de sesión** (concurrencia, lente ADR-008); mientras tanto, si CI la pisa, re-lanzar el job y no subir la holgura del test sin investigar. |
| 2026-07-17 | Las 6 dianas de `fuzz_test.go` (3 min/diana, corpus acumulativo) + reproducción dirigida de la semilla staged `FuzzDecodeInputChunkSplit/5651d63388c96771`. Race: 10 tandas `-race -shuffle=on -count=1` sobre `internal/runtime` (troceadas en 5 llamadas). govulncheck v1.1.4, toolchain go1.26.5: **0 vulnerabilidades alcanzables** (1 CVE solo-importada no alcanzada, igual que 2026-07-11). Mutación: `sse.go` (rotación, no `diff.go` — así lo dejó la fila 2026-07-10), 31 mutantes. | **Bug de código real confirmado en `tty.go` (`decodeInput`)**: cuando un chunk termina en `\r` solo (sin el `\n` que sigue), el driver lo trata como `enter` completo en vez de dejarlo pendiente esperando el siguiente read — a diferencia de cómo sí trata ESC solitario o UTF-8 incompleto a final de buffer. Si `\r` y `\n` llegan en chunks separados se emiten DOS `enter` en vez de uno, violando CP-7 (la misma disciplina que `sse.go:123-124` sí aplica al CR). Corpus de la semilla queda staged. **Hallazgo nuevo de concurrencia**: 3/10 tandas de race rojas por una MISMA familia de fallo — subtests de `chat_test.go`/`chat_p26_test.go` leen la global Lua `C` como `nil`/`false` de forma no determinista bajo `-shuffle`, indicando una carrera real sobre estado compartido entre subtests paralelos; no es la flake de `TestSearchGrepEarlyStopNoLeak` (esa no apareció en ninguna tanda; su fix ya vive en la rama `claude/fuga-grep`, sin mergear en este worktree). Fuzzing: todas las dianas PASS, sin corpus nuevo de fallo, corpus acumulado sigue creciendo (no aplica aún subir fuzztime). Mutación `sse.go`: 31/31 KILLED, 1 TIMED OUT (`ARITHMETIC_BASE` en sse.go:128:9, no diagnosticado como equivalente ni como lento por diseño), 100% eficacia, 0 LIVED. | Sin arreglos de código (la skill detecta, no opera). Pendientes para sesión posterior, por gravedad: (1) bug de `tty.go`/`decodeInput` con CR partido entre chunks (CP-7) — corpus de regresión ya staged; (2) carrera en la global Lua `C` entre subtests de `chat_test.go`/`chat_p26_test.go` bajo `-race -shuffle` — candidata a `G##` o fix directo, lente ADR-008; (3) revisar si el `TIMED OUT` de `ARITHMETIC_BASE` en `sse.go:128:9` es mutante equivalente/lento legítimo antes de la próxima rotación. Próxima rotación de mutación: NO `sse.go` ni `diff.go` (p. ej. `text.go`/`markdown.go`). Recordatorio: mergear `claude/fuga-grep` cierra la flake de 2026-07-11. |
