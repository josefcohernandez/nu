# Plan de migración de la VM: de gopher-lua a PUC-Lua sobre wazero

Ejecuta [ADR-019](adr.md#adr-019--la-vm-objetivo-del-kernel-es-puc-lua-sobre-wazero-gopher-lua-queda-en-mantenimiento).
Evidencia técnica y números: [spike/lua-wasm/INFORME.md](../spike/lua-wasm/INFORME.md)
(el spike es la semilla de las sesiones M02-M03 y el detector anti-caducidad).
Rama de la migración: **`claude/migracion-vm-wasm`**.

> **▶ Próxima sesión: `M13d-ext`** — cargar las **8 extensiones oficiales** sobre el
> estado wasm vía el loader curado (M13a) y **cablear el input real del tty a
> `FeedInput`** (necesita el Runtime arrancando: el driver de TTY convierte teclas en
> eventos y los inyecta). **Watchdog DM4 HECHO** (count-hook por-corrutina; ver la
> bitácora): un bucle de CPU en una task wasm ya se aborta con EBUDGET en vez de
> colgar, y el Runtime pasa su `sliceBudget` al Pool wasm. Cuando M13d-ext esté, la
> suite dual COMPLETA (`NU_VM=wasm go test ./...`) deja de colgar/fallar y se reactiva
> en CI (hoy el job dual está acotado al sondeo `TestRuntimeWasm*`, que ya incluye
> `TestRuntimeWasmWatchdogEBUDGET`).
>
> **M13d-boot HECHO** (arranque cableado): `Runtime.New` construye el estado wasm y
> registra el CATÁLOGO COMPLETO ya portado cuando el backend es wasm; `EvalString`/
> `EvalTaskString` enrutan a wasm. `nu -e` funciona sobre wasm con el catálogo REAL
> (`NU_VM=wasm nu -e 'return nu.version.api'` → `2`; `... 'return nu.sys.platform()'` →
> la plataforma). Un test de integración dedicado (`vmwasm_runtime_test.go`) prueba el
> stack real a través del Runtime (version/sys/codecs por EvalString; fs ⏸, http real y
> concurrencia nu.task por EvalTaskString). Falta para la suite dual completa: DM4 + las
> 8 extensiones. El **catálogo M13b está COMPLETO**:
> codecs, re, fs, sys, log, text (width/truncate **+ wrap/markdown/highlight/diff**,
> M13c), http.request, http.stream, ws, search y proc, más el **despacho de
> métodos-de-handle ⏸ validado** y el **compositor real + Blocks de nu.text enchufados
> a `UIBackend`** (M13c). El patrón está fijado (ver M13b): el catálogo wasm vive en
> `internal/runtime` (junto a sus gemelos gopher, reusando las mismas librerías Go),
> en ficheros con **prefijo** `vmwasm_<módulo>.go` — NUNCA sufijo `_wasm.go` (ver el
> ⚠️ de abajo).
>
> **⚠️ Gotcha de Go (documentado, importante):** un fichero llamado `*_wasm.go`
> recibe una **restricción de build implícita `GOARCH=wasm`** (igual que `_linux`,
> `_amd64`): Go lo EXCLUYE del build normal (linux/amd64) **sin error** —el binario
> compila como si el fichero no existiera, y sus tests no corren pero `go test`
> dice "ok"—. Por eso el catálogo wasm usa el prefijo `vmwasm_` y jamás el sufijo
> `_wasm`. Aplica a cualquier sufijo que sea un GOOS/GOARCH.
>
> **Descomposición de M13 (decisión anotada):** M13-como-estaba mezclaba el
> *mecanismo* del loader (M13a, hecho) con la *integración completa del Runtime*
> (cablear el catálogo real de primitivas, el compositor real y las 8 extensiones,
> y pasar toda la suite con `NU_VM=wasm`), que es el grueso de la migración y
> excede una sesión. Se parte en: **M13a** loader curado (hecho) · **M13b**
> catálogo real (re-expresar fs/proc/sys/http/ws/search/text/re/json/toml/yaml/log
> como HostFn de vmwasm contra el núcleo VM-agnóstico de sus implementaciones Go —
> el volumen mecánico que M09 difirió aquí) · **M13c** enchufar el compositor real
> (compositor.go) a `UIBackend` y el input real del tty a `FeedInput` · **M13d**
> `Runtime.New` ramifica por backend a wasm, carga las 8 extensiones vía el loader,
> y la suite pasa con `NU_VM=wasm` (skips → VACÍA). Pendiente arrastrado: watchdog
> por época (DM4).
>
> **Hallazgo de M13a (anotado):** un prototipo del arranque headless (`nu -e`)
> sobre wasm **funciona** manualmente (`NU_VM=wasm nu -e 'return nu.version.api'`
> → `2`, con `nu.version` inyectado por el preludio), pero enrutar el `EvalString`
> del backend wasm en la **suite dual** (`NU_VM=wasm go test ./...`) la **cuelga**:
> el estado wasm principal aún NO tiene watchdog (DM4) ni el catálogo real, así que
> un test de bucle de CPU (que en gopher aborta el watchdog) gira para siempre. Por
> eso el arranque del binario sobre wasm es **M13d de pleno derecho** (exige el
> watchdog y el catálogo antes de enrutar `EvalString`/la suite dual a wasm), no un
> añadido acotado; el prototipo se revirtió para no desestabilizar CI, dejando
> preparado `Pool.SetAPIVersion` + `nu.version` en el preludio para cuando llegue.
> · Bitácora abajo.
> Censo de la frontera (M01, cerrado): [migracion-vm-censo.md](migracion-vm-censo.md).

---

## 0. Protocolo de sesión (OBLIGATORIO para el agente ejecutor)

Mismo protocolo que [implementacion.md](implementacion.md), adaptado. Si
arrancas sin más contexto que el repo: lee primero `CLAUDE.md`, después este
documento entero, después ADR-019 y el INFORME del spike. Luego:

1. **Antes de tocar nada**: lee el puntero ▶ (arriba) y la última fila de la
   bitácora (abajo). Implementa **solo** esa sesión. Respeta el grafo de
   dependencias (§4): no abras una sesión cuyas dependencias no estén cerradas.
2. **La API sagrada no se toca.** Esta migración cambia el *motor*, jamás la
   superficie `nu.*` ([api.md](api.md)): mismas firmas, mismas semánticas,
   mismos códigos de error. La única excepción, ya decidida en ADR-019 pieza 2,
   es el baseline de lenguaje (api.md §1.2: "Lua 5.1" → "Lua 5.4"), que se
   aplica en M14 y solo en M14. Si una sesión descubre que algo NO puede
   implementarse con la misma semántica observable, **párate**: es una grieta
   G## que se registra en [problemas.md](problemas.md) y se resuelve en los
   documentos antes de seguir.
3. **La suite dual es la ley** (§3). Toda sesión de las fases B-C termina con
   `go build ./...` verde y la parte correspondiente de la suite verde **en los
   dos backends**. Una sesión que deja rojo el backend gopher es una regresión:
   no se avanza el puntero.
4. **Al terminar, en el mismo commit que la feature**: avanza el puntero ▶,
   añade fila a la bitácora, y si cierras fase ejecuta su checkpoint 🔎 (si
   falla, el puntero no se mueve). Commit en español citando la sesión
   (`M05: ...`). Push a la rama de la migración; nunca a otra sin permiso.
5. **Los hitos de veto (§5) son vinculantes.** Si un veto dispara, se para, se
   registra el resultado en la bitácora y en ADR-019 (nueva entrada ADR si
   supone cambiar la decisión), y se consulta al humano. No se "aprieta hasta
   que pase".

## 1. Objetivo y forma de la migración: patrón estrangulador

No es un big-bang: el backend wasm se construye **en paralelo** al actual,
detrás de un selector, y la suite existente se ejecuta contra ambos. La
conmutación (M16) y la retirada (M17) solo llegan cuando la paridad es total y
los vetos de rendimiento pasan.

```
Runtime ──> backend gopher-lua  (el actual; intacto hasta M17)
       └──> backend wasm        (nuevo: wazero + lua.wasm + puente)
                 selector: nu.toml [vm] backend = "gopher"|"wasm"
                 y para tests: variable de entorno NU_VM (M04)
```

Piezas heredadas del spike (`spike/lua-wasm/`), que NO se copian a ciegas sino
que se promueven con calidad de kernel: el build de `lua.wasm` (PUC-Lua 5.4.7
sin parches + `spike_unwind.h`), el trampolín de desenrollado sobre
`Snapshot/Restore`, y las lecciones documentadas (la no-reentrancia de
`api.Function`, el coste del yield, el stub de `setjmp.h`).

## 2. Decisiones ya tomadas (no re-litigar) y decisiones de entrada

**Cerradas por ADR-019:** dirección (PUC-Lua sobre wazero), baseline Lua 5.4,
puente ⏸ por corrutinas nativas, gopher-lua en mantenimiento hasta M17.

**Decisiones que el plan fija ahora** (anotadas aquí; el ejecutor las sigue
salvo contraorden del humano):

| # | Decisión | Racional |
|---|---|---|
| DM1 | El blob `lua.wasm` **se comitea** en el repo (`internal/vmwasm/lua.wasm`) junto a su `build.sh` reproducible y una nota de licencia (MIT de PUC-Lua, compatible Apache-2.0/ADR-013); un job de CI reconstruye y compara hash para que blob y fuentes no deriven | CI y contribuidores no necesitan clang/wasi-libc; la reproducibilidad queda blindada por el job |
| DM2 | Selector: `nu.toml [vm] backend` + env `NU_VM` (tests). Default `gopher` hasta M16 | Estrangulador clásico; el flip es un cambio de default, no un merge gigante |
| DM3 | Los valores cruzan la frontera wasm como **copias JSON-ables + handles enteros** para userdata (Task, Proc, Region, Block...) con despacho de métodos vía host functions | Coincide con el modelo mental que api.md ya impone (handles opacos; workers ya cruzan solo JSON-ables) |
| DM4 | El watchdog wasm usa la **interrupción por época** de wazero (presupuesto por slice) en vez del mecanismo actual | Es el equivalente natural y más barato; su semántica observable (EBUDGET, no capturable) debe ser idéntica — 🔒 |
| DM5 | `require`/loader: se implementa sobre el estado wasm el MISMO cargador curado de hoy (rutas de plugins, unicidad de nombre); la lib `package` de PUC no se abre | El loader es del kernel, no de la stdlib; idéntico a la decisión del sandbox actual |

## 3. Política de tests: la suite dual es la columna vertebral

- **El mecanismo (se construye en M04):** el arnés de tests de
  `internal/runtime` gana un selector por env (`NU_VM=wasm go test ./...`).
  El CI corre la suite completa con ambos valores desde M04 (la de wasm,
  limitada a lo ya migrado: se mantiene una lista de skip explícita que cada
  sesión RECORTA — nunca amplía).
- **Inventario 🔒 heredado**: todos los tests que hoy blindan semántica de VM
  son de paso obligatorio en wasm — G31 (scheduler), G41 (upvalues vivos:
  `TestG41*` deben pasar en wasm *sin* el blindaje, porque el bug no existe en
  PUC), watchdog (EBUDGET), cancelación (aborto no capturable a través de
  `pcall`), workers (caps, colas acotadas, exclusión recv/on_message), errores
  estructurados (§1.4), y los checkpoints CP existentes.
- **Nuevos 🔒 de esta migración**: el puente (yield/resume con valores, yield a
  través de pcall — la prueba de que ADR-011 muere), el marshaling (UTF-8
  estricto G11, `nu.json.NULL`, handles inválidos → error accionable), y la
  paridad de códigos de error módulo a módulo.
- Los benchmarks del spike se promueven a `internal/vmwasm/bench_test.go` y se
  corren en los checkpoints (no en cada push).

## 4. Las sesiones

### Fase A — Cimientos (la "interfaz de VM" de ADR-019 fase a)

| Sesión | Contenido | Depende de |
|---|---|---|
| **M01** | **Censo de la frontera VM.** Inventario mecánico (script `tools/censo-vm.sh` + tabla en este doc o anexo) de cada símbolo de gopher-lua usado por fichero del kernel, clasificado en: valores, registro de funciones, threads/corrutinas, errores, userdata, otros. Es el mapa de M05-M13; lo que no salga aquí no existe. Sin código de producción | — |
| **M02** | **`internal/vmwasm`: el blob productivo.** Promover el build del spike: `build.sh` reproducible + `lua.wasm` comiteado (DM1) + `go:embed` + shim C consolidado (buffer, exports, unwind) + nota de licencia + job de CI de hash. El shim gana lo que el spike no tenía: `require` hook (DM5), tabla de handles (DM3), multi-instancia limpia | — |
| **M03** | **El puente de desenrollado, calidad kernel.** Trampolín Snapshot/Restore endurecido: LIFO auditado, `__stack_pointer`, funciones frescas (no-reentrancia), detección de traps reales vs throws, y el gate-test del spike promovido a test 🔒. Multi-instancia: N módulos wasm conviviendo (preparación de workers M12) | M02 |

### Fase B — El runtime paralelo (estrangulador)

| Sesión | Contenido | Depende de |
|---|---|---|
| **M04** | **Backend seleccionable + boot desnudo.** `Runtime` acepta backend (DM2); el estado wasm arranca sin plugins con el sandbox curado; el arnés de tests gana `NU_VM` y la lista de skips; CI dual desde aquí | M03 |
| **M05** | **Marshaling + registro de host functions.** La infra genérica para exponer primitivas Go al estado wasm: copias JSON-ables, strings sin re-codificar (G11), errores estructurados `{code,message,detail}` cruzando fielmente (§1.4), tabla de handles con ciclo de vida (DM3). Equivalente wasm de `registerNu`. 🔒 exhaustivo: es la pieza de la que cuelga todo | M04 |
| **M06** | **ADR-020 (diseño del puente definitivo) + scheduler por corrutinas.** PRIMERO el ADR: task = corrutina Lua nativa; ⏸ = yield con petición; el loop Go resume con el resultado; presupuesto del coste de yield con las vías del INFORME §4.1 evaluadas y una elegida. DESPUÉS el código: `nu.task` completo (spawn/sleep/all G27/race/every/defer/future/await/cleanup) sobre corrutinas. ADR-020 **reemplaza** a ADR-011 (se marca allí). 🔒 paridad scheduler_test/allrace/future/timers | M05 |
| **M07** | **Cancelación y watchdog.** Aborto no capturable a través de `pcall` (ahora SIN wrapper especial: el throw de PUC + un marcador propio bastan — diseño en ADR-020) y watchdog por época de wazero (DM4). 🔒 paridad cancel/watchdog (S08/S09) | M06 |
| **M08** | **Bus de eventos, input y lo síncrono.** `nu.events` (G10: encolado en anchura), handlers síncronos, timers. 🔒 paridad events_test — incluida la semántica de G41 SIN blindaje | M06 |
| **M09** | **Primitivas de IO y datos.** fs/proc/sys/http/ws/search/text/re/json/toml/yaml como host functions sobre la infra de M05 (mecánico; en bloque). 🔒 paridad de sus tests módulo a módulo; la lista de skips baja en masa aquí | M05 |
| **M10** | **Userdata como handles.** Task, Proc, Stream, Ws, Watcher, Timer, Future con métodos despachados por host functions y `cleanup`/GC coherentes (la regla "quien crea, mata" + red del GC). 🔒 paridad proc/stream/ws | M09 |
| **M11** | **UI: compositor, regiones, bloques, input.** Region/Block como handles (los Blocks siguen viviendo en Go — hoy ya son opacos); keymaps y pila de input. Criterio de hecho: la pantalla desnuda (G21) y el chat arrancan en wasm. 🔒 paridad compositor/input/toolkit/chat | M10 |
| **M12** | **Workers = instancias wasm.** Un worker por instancia (aislamiento FÍSICO de memoria — el regalo de ADR-019); caps = lista de host functions concedidas (deny-by-default para superficie nueva, G6); colas acotadas con backpressure; exclusión recv/on_message (G8). 🔒 paridad worker_test + nota en pospuesto.md: P2 gana camino natural | M09 |
| **M13** | **Loader, plugins y extensiones oficiales — INTEGRACIÓN con el Runtime.** `require` curado sobre wasm (DM5), carga topológica, reload best-effort (G2), y las 8 extensiones embebidas corriendo. Criterio de hecho: `nu --default-config && nu` funciona entero en wasm. La lista de skips queda VACÍA. **Descompuesta** (ver bitácora M13a y la nota de descomposición abajo) en: **M13a** loader curado (mecanismo, hecho) · **M13b** catálogo real (fs/proc/http/... como HostFn contra el núcleo VM-agnóstico del kernel) · **M13c** compositor + input reales enchufados a `UIBackend`/`FeedInput` · **M13d** `Runtime.New` ramifica a wasm, carga las 8 extensiones y la suite pasa con `NU_VM=wasm` (skips VACÍA) | M08, M10, M11 |

### Fase C — Paridad total, veto y conmutación

| Sesión | Contenido | Depende de |
|---|---|---|
| **M14** | **Baseline Lua 5.4** (ADR-019 pieza 2): api.md §1.2 actualizado, barrido de `unpack`/`setfenv`/etc. en extensiones y ejemplos de docs, `nu.version.api` NO se mueve (el baseline no es una firma). Guía de plugins gana la nota de compatibilidad | M13 |
| **M15** | **Checkpoint integral 🔎 + HITO DE VETO (§5).** Suite completa `-race` verde en ambos backends; benchmarks comparados registrados en la bitácora; los tres vetos evaluados con números | M14 |
| **M16** | **La conmutación.** Default = wasm; gopher queda tras `backend = "gopher"` (legacy, un ciclo de gracia). README/arquitectura.md/CLAUDE.md coherentes. El onramp no cambia (superficie CLI intacta) | M15 |
| **M17** | **La retirada.** Se elimina gopher-lua del go.mod, mueren los blindajes que solo existían por él (G41 en cancel.go, el pcall envuelto especial si ADR-020 lo hizo innecesario), ADR-011 se marca "Reemplazada por ADR-020", bitácoras y docs cerrados. El binario queda con UNA VM | M16 + un ciclo de uso real del default wasm sin regresiones |

### Grafo (resumen)

```
M01 ─┐
M02 ─┴─ M03 ── M04 ── M05 ─┬─ M06 ─┬─ M07
                           │       └─ M08 ─┐
                           ├─ M09 ─┬─ M10 ─┼─ M11 ─┐
                           │       └─ M12  │       ├─ M13 ── M14 ── M15 ── M16 ── M17
                           └───────────────┴───────┘
```

## 5. Hitos de veto (vinculantes, se evalúan en M15)

Como el spike de ADR-007/ADR-012: criterios objetivos pactados ANTES de
empezar. Si alguno falla, la migración se PAUSA en M15 (el trabajo no se tira:
queda detrás del selector), se registra, y decide el humano.

1. **Corrección**: la suite completa con `-race`, verde en wasm, incluidos
   todos los 🔒 heredados y nuevos. Sin excepciones ni skips.
2. **Rendimiento del camino caliente**: la simulación del streaming
   (modelo-ejecucion.md: SSE → markdown → blit) y un turno de agente headless
   contra el adaptador stub quedan **dentro de 2×** del backend gopher; el
   ciclo yield+resume del puente definitivo, **≤ 50 µs** sostenido.
3. **Experiencia**: arranque interactivo (`nu` con el conjunto oficial) sin
   degradación perceptible (< 150 ms añadidos con el módulo precompilado en
   caché) y binario final ≤ +1,5 MB sobre el actual.

## 6. Riesgos vigilados (con dueño)

- **API experimental de snapshots de wazero** → el gate-test 🔒 de M03 y el pin
  de versión; un upgrade que la rompa se detecta en CI, no en producción.
- **Coste del yield** (INFORME §4.1) → se decide y presupuesta en el ADR-020
  de M06; el veto 2 lo audita con números en M15.
- **Deriva blob/fuentes** → el job de hash de M02 (DM1).
- **Los tests que usan globales por la vieja limitación de upvalues** siguen
  siendo válidos (los globales funcionan igual); no se reescriben en masa.

---

## Bitácora

| Fecha | Sesión | Resumen |
|---|---|---|
| 2026-07-06 | **DM4** (watchdog) | El watchdog por slice del backend wasm (🔒, api.md §1.3): abortar la task que quema CPU sin ceder (`while true do end`) con **EBUDGET no capturable**, dejando el estado vivo y corriendo sus cleanups. **Desviación del mecanismo DM4:** NO se usa la "interrupción por época" de wazero —cancelar el ctx de un Call cierra el MÓDULO ENTERO (mata el estado), no una task, y todas las tasks viven en un único `nu_sched_step` Call—; en su lugar, un **count-hook de PUC-Lua por-corrutina**. **Mecanismo final:** (1) `nu_shim.c` gana `wd_hook` (instalado con `lua_sethook`/`LUA_MASKCOUNT`, WD_COUNT=10000 instrucciones) que cada tramo llama al import host `nu_over_budget` y, si el slice rebasó su deadline, **cede** (`lua_yield`); y `__wd_arm()` (función C global, porque el baseline no abre la lib debug) que instala el hook en el hilo que la llama —el preludio lo invoca al principio del cuerpo de CADA task (nu.task.spawn), pues un hilo nuevo no hereda el hook en 5.4—. (2) `nu_over_budget` (Go, `hostOverBudget` en vmwasm.go, registrado en el módulo host junto a host_try/host_dispatch) compara `time.Now()` con `inst.taskDeadline`; **race-free** (mismo goroutine que conduce el Call, invocación síncrona; ninguna goroutine de fondo lo toca). (3) **Deadline por slice** (paridad con gopher): el scheduler Lua (`__resume`, preludioSched) llama a `__reset_budget()` —primitiva síncrona registrada en `newBarePool` vía `registerWatchdog`, presente en todo Pool— ANTES de cada `coroutine.resume`, fijando `inst.taskDeadline = now + inst.sliceBudget`. (4) **Aborto:** tras `coroutine.resume`, `__resume` reconoce el aborto por budget porque `yielded == nil` (ver Fase 0), marca la task `{code="EBUDGET"}`, corre sus cleanups LIFO (`__finish`) y NO la reencola —gemelo del aborto por cancelación `t.cancelled`, con EBUDGET—. **`sliceBudget`** vive en el Pool/Instance (`Pool.SetSliceBudget`, copiado a la Instance en `NewInstance`); `<=0` DESACTIVA el watchdog (`nu_over_budget` siempre 0) —como en gopher y en los workers (G15)—. **Resultado de la FASE 0 (de-risking, clave):** un count-hook de Lua 5.4 **SÍ** puede ceder a través del trampolín Snapshot/Restore (M03) **y** del pcall (el yield del hook es un `luaD_throw(LUA_YIELD)` idéntico al de un `coroutine.yield` normal, que ya atraviesa el pcall base yieldable sin frame de try intermedio → aborto NO capturable). **Hallazgo importante:** un count-hook **NO puede ceder valores** —`luaD_hook` restaura el `top` de la pila tras ejecutar el hook, así que cualquier valor empujado se pierde—; por eso el yield es de **0 valores** y el scheduler lo distingue por `yielded == nil` (todos los ⏸ normales ceden una tabla `{op=...}`), en vez del `{op="__budget"}` que el diseño preveía. **Integración con el Runtime:** `buildWasmState` pasa `rt.sched.budget` al Pool wasm con `SetSliceBudget` —un bucle de CPU en una task wasm por el Runtime real aborta con EBUDGET (verificado por binario: `NU_VM=wasm nu -e 'nu.task.spawn(function() while true do end end)'` termina en ~100ms en vez de colgar)—. **5 tests** (🔒, `-race`): `TestWatchdogAbortaBucleCPU` (el corazón: el bucle de CPU se aborta y OTRA task sigue corriendo, estado vivo), `TestWatchdogNoCapturablePorPcall` (el bucle en un `pcall` NO lo captura ese pcall; el awaiter observa EBUDGET), `TestWatchdogCorreCleanups` (cleanups LIFO al abortar por budget), `TestWatchdogDesactivado` (sliceBudget<=0: bucle acotado completa, G15), y `TestRuntimeWasmWatchdogEBUDGET` (EBUDGET por el Runtime real por EvalTaskString+await, acotado con tope de wall-clock). Todos con un slice pequeño (30 ms) y tope de wall-clock (`runTasksBounded`) para que un watchdog roto sea un FALLO, no un cuelgue del CI. Blob rebuild: `nu.wasm` 477920→**478084** bytes, sha256 `1991d3e7…`. Verificación: `go build`/`go vet` limpios, blob reproducible (hash estable ×2), `internal/vmwasm` verde entero (`-race`), `TestRuntimeWasm*` verde, suite gopher completa verde, `censo-vm.sh --check` OK. **Pendiente:** M13d-ext (8 extensiones + input tty) para reactivar la suite dual completa. Puntero sigue en **M13d-ext**. |
| 2026-07-06 | **M13d-boot** | Arranque del Runtime sobre el backend wasm cableado (crux de M13d, sin las extensiones ni el watchdog, que son sesiones aparte). **`runtime.go`:** `New`, cuando `rt.vmBackend == VMWasm`, tras armar el `rt` gopher habitual (transición del estrangulador: el catálogo wasm reusa `rt.fs`/`rt.http`/`rt.sys`/`rt.ui`/`rt.log`/`rt.sched`), construye el estado wasm vía `buildWasmState` → `vmwasm.NewPool()` + `SetAPIVersion(APILevel)` + `registerWasmCatalog(p)` (agrupa los 11 `registerXWasm` del catálogo COMPLETO ya portado: codecs/re/fs/sys/log/text[+Blocks]/http[+stream]/ws/search/proc/ui —`registerUIWasm` sólo instala el compositor si `rt.ui != nil`, gating headless G20) + `p.NewInstance()`. Campos nuevos del struct: `wasmPool *vmwasm.Pool`, `wasm *vmwasm.Instance`, `wasmErr error`. **Un fallo de construcción NO rompe la firma sagrada de `New`** (que no devuelve error): se aparca en `rt.wasmErr` y lo propagan `EvalString`/`EvalTaskString` al primer eval (como el `configErr` aplazado a `Boot`). `Close` cierra `rt.wasm` (`Instance.Close`) y `rt.wasmPool` (`StopWorkers`+`Close`) si se construyeron. **`eval.go`:** al inicio de `EvalString`/`EvalTaskString`, `if rt.vmBackend == VMWasm { return rt.evalStringWasm/evalTaskStringWasm(code) }` — el enrutado SÓLO bajo VMWasm, así el backend gopher (default hasta M16) no cambia y su suite sigue verde. `evalStringWasm`: corre el chunk en el ESTADO PRINCIPAL (`inst.Eval`, no task —igual que `nu -e` en gopher, §1.3), `RunTasks` para drenar las tasks que lance (como `waitIdle`), devuelve `[]string{result}` o nil si vacío. `evalTaskStringWasm`: corre el chunk COMO TASK envolviéndolo en `nu.task.spawn(function() ... end)` con un `pcall(table.pack(...))` que captura su desenlace (primer valor de retorno o error) en globales —la task nunca lanza, así el error no escapa de `RunTasks`—; tras drenar, un sondeo lee el desenlace codificado (delimitado por 0x01). **El error estructurado cruza FIEL por `EvalTaskString`** (se lee la tabla `{code,message}` en Lua); por `EvalString` el puente sólo expone el texto del error (la tabla se popea en `nu_eval`), best-effort → `wasmChunkError` recupera un `*StructuredError` sólo si el texto es "CODE: msg" con code reservado, si no un error simple. Multi-valor y cruce 100% fiel por EvalString se afinan si un test lo exige (anotado). **Test dedicado `vmwasm_runtime_test.go`** (7, verde): prueba el stack REAL por `New(WithVMBackend(VMWasm), ...)` headless — `nu.version.api`→2, `nu.sys.platform()` no vacío, round-trip `nu.json` (EvalString, catálogo síncrono); fs write/read por disco (⏸ real), `nu.http.request` contra `httptest` (red real), concurrencia `nu.task.all`+`sleep` intercalada, y ENOENT estructurado de `nu.fs.read` (EvalTaskString). **CI (`ci.yml`):** el job "Suite DUAL (NU_VM=wasm)" ya NO corre `NU_VM=wasm go test ./...` entero (colgaría: EvalString enruta a wasm y hay bucles de CPU sin watchdog DM4); acotado a `go test -run 'TestRuntimeWasm' ./internal/runtime/` (el paquete `internal/vmwasm` ya lo cubre otro paso del job). Nota en el YAML: la suite dual COMPLETA se reactiva con DM4 + M13d-ext. **Hallazgo (EvalTaskString):** el chunk-como-task se envuelve con `pcall` porque el scheduler Lua captura los errores POR task (no escapan de `RunTasks`); el `yield a través de pcall` de PUC-Lua (ADR-020, imposible en gopher/G31) lo hace transparente para los ⏸ dentro del `pcall`. Verificación: `go build`/`go vet` limpios, binario `NU_VM=wasm nu -e` da `2`/`linux`, gopher sigue en `2`; suite gopher completa verde (×4, incluido `-shuffle`), `internal/vmwasm` verde. Puntero → **M13d-ext** (extensiones + input tty; DM4 arrastrado). |
| 2026-07-06 | **M13c** | Compositor REAL enchufado a `vmwasm.UIBackend` + los Blocks de `nu.text` (§9-§10). Cierra M13b: el catálogo queda COMPLETO. `vmwasm_ui.go`: `compositorBackend` envuelve el `*compositor` (el de `rt.ui.comp`) e implementa `UIBackend` reusando el compositor REAL (no reimplementa nada) —`Size`/`Caps` (por `detectColors`, como gopher)/`NewBlock` (parser VM-agnóstico `parseLinesWasm`/`parseStyleWasm`/`normalizeColorWasm`, espejo de parseLine/parseStyle/normalizeColor desde `[]any`)/`NewRegion` (`comp.addRegion`)/`ClipboardSet`/`ClipboardGet` (OSC 52, reusa `encodeOSC52*`/`readOSC52Reply`)—; y `regionAdapter` traduce los 11 métodos de `RegionObj` a los reales de `*uiRegion` (`move`/`resizeRegion`/`raise`/`lower`/`show`/`hide`/`release`, `content.blitBlock`/`fill`, `setCursor` con `off = !show`). `registerUIWasm(p, rt)` instala el backend SÓLO si `rt.ui != nil` (gating headless G20). **Único toque al gopher (adición pura):** `*block` gana `Dims() (int,int)` (block.go) para satisfacer `vmwasm.BlockObj` —así el handle "Block" resuelve a su `*block` y `Region:blit` copia su ventana (G28)—; no cambia el camino de gopher. `vmwasm_text.go`: `wrap`/`markdown`/`highlight`/`diff` como HostFn síncronos que reusan ÍNTEGRO el núcleo Go (`wrapText`, `renderMarkdownBlocks`, `highlightToBlock`, `computeDiff`/`renderDiffBlock`) y devuelven el Block vía `inst.AllocHandle("Block", blk)` como `{id,width,height}`; un wrapper Lua (AddPreludio) lo envuelve como handle opaco con `.width`/`.height` (misma forma que `nu.ui.block`, usando `__handle_mt` del preludio base). `diff` devuelve `{hunks, block?}` (`opts.render`); themes de markdown/diff parseados con `applyMarkdownThemeWasm`/`applyDiffThemeWasm` reusando `parseStyleWasm`. **Decisión anotada:** `RegionObj.Fill`/`Resize` (interfaz M11) no tienen canal de error, así que un estilo mal formado se degrada a nil y un tamaño negativo se clampa a 0 —a diferencia del `Region:fill`/`resize` de gopher que lanzan EINVAL—; es una limitación del binding M11, no de esta sesión. 18 tests (`vmwasm_ui_test.go` + añadidos a `vmwasm_text_test.go`) con el compositor REAL: size/caps reales, region+blit observando la rejilla compuesta (`composeRow`), ciclo de vida (move/resize/raise/lower/hide/show/destroy→ECLOSED, región descolgada del compositor), cursor último-gana (secuencia ANSI del frame), **blit de un Block de `text.wrap`** (round-trip del handle "Block"), **G28 offset negativo recorta sin panic**, la tubería REAL de pintado (`paint`→ANSI con el glifo), fill con color literal (SGR truecolor en el frame), y smokes de wrap/markdown/highlight/diff (dims coherentes, EINVAL de wrap/markdown, forma de diff con/sin render). Verde con `-race`; suite gopher completa y `internal/vmwasm` siguen verdes. **Pendiente para M13d:** el input REAL del tty→`FeedInput` (necesita el Runtime arrancando: el driver convierte teclas en eventos). Puntero → M13d. |
| 2026-07-03 | — (plan) | Nace este plan (ejecuta ADR-019; rama `claude/migracion-vm-wasm`). Puntero en M01. |
| 2026-07-03 | **M01** | Censo de la frontera VM cerrado: `tools/censo-vm.sh` (resumen/`--files`/`--check`) + [migracion-vm-censo.md](migracion-vm-censo.md) con las 6 categorías (C1 valores/marshaling, C2 host functions, C3 puente ⏸, C4 errores/desenrollado, C5 userdata/handles, C6 libs/baseline) y el mapa fichero→categoría→sesión. La guardia `--check` cableada en CI (trinquete: ningún símbolo gopher-lua nuevo). Hallazgo confirmado del censo: C4 no se traduce, se **borra** (cancel.go + blindaje G41 existen solo por defectos de gopher-lua). Sin código de producción. Puntero → M02. |
| 2026-07-03 | **M02** | Blob productivo `internal/vmwasm`: shim consolidado (`shim/nu_shim.c`, renombrado de `spike_*` a `nu_*`, dispatch host genérico en vez de los host functions de benchmark del spike), `build.sh` reproducible (honra `$CC`), `nu.wasm` (477 KB) **comiteado** + `go:embed`, nota de licencia MIT (compatible Apache-2.0, ADR-013) y `.gitignore` de las fuentes de Lua. Cargador Go: `Pool` (compila una vez) + `Instance` (N instancias, **memoria aislada** — base de los workers M12), trampolín Snapshot/Restore heredado del spike, y `Dispatcher` pluggable (la costura que M05 rellena; en M02 rechaza). 7 tests: boot 5.4, libs del baseline, recuperación de errores, **G41 semántica de referencia sin blindaje** 🔒, **yield a través de pcall** 🔒, multi-instancia aislada, costura del dispatcher. Job de CI `vmblob` (reconstruye y verifica que el blob no derivó, DM1). wazero pasa a dependencia directa. Puntero → M03. |
| 2026-07-03 | **M03** | Trampolín endurecido a calidad de kernel (`trampolin_test.go`, 🔒 con `-race`): (1) anidamiento profundo de pcalls con LIFO balanceado; (2) **trap real del motor se propaga como fallo duro** (export de test `nu_selftest_trap` → `__builtin_trap`), jamás se traga como throw; (3) la no-reentrancia de `api.Function` esquivada (funciones frescas por llamada); (4) **N instancias concurrentes en goroutines sin contaminación cruzada** (ctx-routing — la base de M12); (5) error tras yield dentro del pcall que lo cruzó. **Hallazgo (anotado):** el techo de llamadas C de Lua (LUAI_MAXCCALLS ≈ 200) NO lo baja el trampolín de forma apreciable (150 niveles OK) y **degrada con gracia** — rebasarlo es un error de Lua capturable ("C stack overflow"), no un trap, y el estado sobrevive (semántica idéntica al Lua nativo). Puntero → M04. |
| 2026-07-03 | **M04** | Backend seleccionable (DM2): `VMBackend` (gopher/wasm) en `vm_backend.go`, `nu.toml [vm] backend`, env `NU_VM`, Option `WithVMBackend` y método `Runtime.VMBackend()`, con precedencia Option > `NU_VM` > `nu.toml` > gopher (default seguro hasta M16). `New` resuelve y registra el backend; el **camino de arranque wasm paralelo lo cablean M05-M13** (hoy `New` construye siempre gopher). Infra de la **suite dual**: helper `skipIfWasm` (la costura de la lista de skips, que M05-M13 recortan) y job de CI `Suite DUAL (NU_VM=wasm)` que corre `go test ./...` con el backend seleccionado. 4 tests del selector (default/env/toml/precedencia). La suite completa pasa con `NU_VM=wasm` (a M04, sigue sobre gopher por debajo: el selector sólo fija el campo). Puntero → M05. |
| 2026-07-06 | **M13b** (proc) | `nu.proc` sobre wasm (§6, inventario 🔒). `registerProcWasm` cuelga las tres funciones del módulo —`proc.run` (⏸: la conveniencia con buffers, todo el ciclo start+IO+wait en la goroutine de fondo vía `runBuffered`), `proc._spawn` (**síncrona**: a diferencia de `ws._connect`/`http._stream`, arrancar NO bloquea —`cmd.Start` hace fork/exec sin esperar—, así que es `p.Register` y crea el handle **Proc** con `inst.AllocHandle` en el hilo principal, donde AllocHandle es seguro) y `proc.alive` (síncrona, G17: existencia no identidad, reusa `pidAlive`)— y los seis métodos del handle Proc: `write`/`read_line`/`read`/`wait` (⏸, IO bloqueante enrutado por `__hcall_s`) y `close_stdin`/`kill` (síncronos, `__hcall`). **Reusa ÍNTEGRO el núcleo VM-agnóstico de proc.go**: `newCmd`/`mergedEnv`/`splitEnv` (entorno con overlay de `setenv` §7), `runBuffered` (start+timeout+captura), y los métodos de `luaProc` (`writeStdin`/`reader`/`readLineFrom`/`readFrom`/`wait`/`killSignal`, con su reparto de candados stdinMu/stdoutMu/stderrMu/killMu + waitOnce, el reaper de fondo y el finalizer del GC): no se reimplementó nada de proceso/pipes/entorno. **Helpers extraídos de proc.go** (VM-agnósticos, compartidos por ambos backends, SIN cambiar el comportamiento gopher): `spawnProc(argv, opts) -> (*luaProc, error)` —el núcleo de `procSpawn`: pipes manuales `os.Pipe` (no `StdoutPipe`, para desacoplar reaping y streaming), `Start`, tracking, reaper y finalizer; devuelve el error CRUDO de arranque que cada backend traduce— y `(*luaProc).closeStdin()` —el cuerpo de `close_stdin`, idempotente—; `procSpawn` (gopher) ahora llama a `spawnProc` y sólo añade el userdata. Añadido nuevo (wasm-side): `parseProcArgsWasm` (gemelo de `parseProcArgs`: argv array-no-vacío-de-strings, opts cwd/env/stdin/timeout_ms desde `map[string]any`, con la MISMA permisividad que gopher —opts/env/entrada/timeout mal tipados se ignoran; sólo argv malo o timeout negativo → EINVAL—), `mapProcStartErrorWasm` (ENOENT/EACCES/EIO, espejo de `mapProcStartError`) y `einvalProc`. Wrapper Lua (AddPreludio, estilo `nu.ws.connect`) que envuelve el handle de `_spawn` y cuelga los seis métodos al despacho correcto. **Guardas de nil (behavior-preserving):** `spawnProc` gatea `rt.sys.envOverlay()` y `rt.sched.trackProc/track` en `!= nil` para admitir el `rt` mínimo de los tests (gopher siempre los tiene → idéntico); la red del GC sigue matando un Proc sin referencias. **Decisión anotada:** la foto del overlay de `setenv` en `proc.run` se toma en la goroutine de fondo del ⏸ (no en el hilo principal como gopher), porque un ⏸ corre entero en el fondo; el resultado observable dentro de una task es idéntico. 9 tests (driver + procesos POSIX reales echo/cat/sh/sleep): spawn+read_line+wait+EOF, cat round-trip por stdin (write/close_stdin), read crudo hasta EOF, **kill(SIGKILL) a un sleep** (pid expuesto por un método de test → alive(pid)=true vivo, wait desbloquea, `waitDead` confirma muerto en Go), **alive G17** (init=true, 2^30=false), spawn inválido (inexistente→ENOENT, argv vacío→EINVAL), `run` (echo code 0+stdout, exit 7 dato, inexistente→ENOENT, timeout→ETIMEOUT), read_line con which inválido→EINVAL, write-tras-close_stdin→ECLOSED. Verde con `-race`; la suite gopher completa sigue verde. Faltan de M13b: text-Blocks. Puntero sigue en M13b. |
| 2026-07-06 | **M13b** (http.stream) | `nu.http.stream` sobre wasm (§8, inventario 🔒). `registerHTTPStreamWasm` (añadido a `registerHTTPWasm`, ya existente) cuelga la primitiva `http._stream` (⏸) —abre la petición SOLO hasta las cabeceras en la goroutine de fondo del scheduler y, ya recibidas, registra el handle **Stream** con `inst.AllocHandle` (mismo idioma que `ws._connect`: primitiva ⏸ que además crea handle, sólo toca la `handleTable`) y devuelve por el wire el handle + `status`/`headers`— y los métodos del handle: `next_chunk`/`next_event` (⏸, enrutados por `__hcall_s`) y `close` (síncrono, `__hcall`). **Reusa ÍNTEGRO el núcleo VM-agnóstico de stream.go** (`openStream`/`newHTTPStream`, la goroutine `readLoop` productora, la cola acotada `nextChunk`/`maxStreamBuffer` con backpressure→EIO, el idle-timeout→ETIMEOUT, el parser SSE 🔒 `sseParser`, `closeOnce`, `classifyTransportError`/`errStreamClosed`): no se reimplementó nada de red/buffer/parseo. **Decisión de diseño (documentada):** el bucle de `events()` —un evento SSE puede llegar partido en N trozos— corre ENTERO en la goroutine de fondo de UN solo `__hcall_s` (`next_event` consume trozos con `nextChunk`, que bloquea sin tocar Lua, hasta cerrar un evento o EOF), a diferencia del backend gopher que suspende una vez por trozo; el resultado observable es idéntico y `st.sse` lo toca sólo el consumidor (una task, secuencial) como en gopher, sin candado. El wrapper Lua (AddPreludio, estilo `nu.ws.connect`) envuelve el handle con los campos `status`/`headers` y expone `chunks()`/`events()` como los **iteradores** que Lua consume con `for x in st:chunks() do` (cada `next` por `__hcall_s`). Añadido nuevo (wasm-side): `parseIdleTimeoutWasm` (opts.idle_timeout_ms desde `map[string]any`, positivo → EINVAL con prefijo `nu.http.stream:`), `sseEventToWasm` (el mapa `{data, event?, id?}` para el wire, espejo de `pushEvent`) y `streamErrWasm` (cierre→ECLOSED, resto→`httpErrWasm`, espejo de `raiseStreamError`); el parseo del resto de opts reusa `parseReqOptsWasm` (el mismo que request —en gopher `parseReqOpts` también lo comparten—). **Único cambio al backend gopher** (behavior-preserving): un nil-guard en `httpStream.close` (`untrackStream` sólo si hay scheduler, calcado del de `luaWs.close`; gopher siempre pasa `rt.sched`, así que idéntico) — la suite gopher sigue verde. 8 tests (driver + servidores locales httptest con `flushServer`): chunks crudos acumulados hasta EOF, SSE con eventos {event,data,id}, evento SSE partido en writes adversos→1 evento, status 404 sin lanzar, close idempotente (×3), **backpressure→EIO** (~12 MiB con consumidor dormido), **idle-timeout→ETIMEOUT** (body mudo), y idle_timeout_ms inválido→EINVAL (2 casos). Faltan de M13b: proc, text-Blocks. Puntero sigue en M13b. |
| 2026-07-06 | **M13b** (search) | `nu.search` sobre wasm (§11). `registerSearchWasm` cuelga las tres primitivas del módulo reusando **ÍNTEGRO el núcleo VM-agnóstico de search.go**: `search.files` (⏸) y `search.fuzzy` (síncrona) como HostFn directos, y el iterador paralelo de `grep` como **handle `GrepIter`** —`search._grep` (⏸) enumera el árbol (IO, cede al scheduler), arranca el pool de goroutines (`newGrepIter`) y registra el iterador con `inst.AllocHandle` (mismo idioma que `ws._connect`: primitiva ⏸ que además crea handle, sólo toca la `handleTable`), y `GrepIter:next` (⏸, enrutado por `__hcall_s`) es el gemelo exacto de la clausura `next` de gopher (corta en `max`, bloquea en el canal `results` en la goroutine de fondo, cuenta `emitted` y cierra el pool en tope/EOF). El wrapper Lua (AddPreludio) envuelve el handle de `_grep` en la **clausura iteradora** que reconstruye el `for r in nu.search.grep(...)` del contrato (el wire no cruza clausuras: DM3 exige el handle). **Reutiliza sin tocar** `walkFiles`/`filesOpts` (recorrido + `.gitignore` G7 + glob/hidden/max), `fuzzyScore`/`fuzzyMatch` (scorer de subsecuencia con bonus, orden estable por score), `grepIter`/`grepResult`/`grepOpts`/`grepFile`/`grepWorkers`/`newGrepIter` (el pool paralelo). Añadido nuevo (wasm-side): `parseFilesOptsWasm`/`parseGrepOptsWasm` (opts desde `map[string]any`, contrato de §11 con EINVAL) y `grepResultToWasm` (el mapa `{path, line_no, line, ranges}` para el wire, rangos byte 1-based como en S26); errores de recorrido reusan `mapFsErrorWasm`. **Único cambio al backend gopher** (behavior-preserving): un nil-guard en `grepIter.close` (`untrackGrep` sólo si hay scheduler, como el de `luaWs.close` en la fila ws; gopher siempre pasa `rt.sched`, así que idéntico) — la suite gopher sigue verde. El rastreo para `Runtime.Close` (`stopAllGreps`) se gatea en `rt.sched != nil`, como en ws; el `cleanup` de la task por cancelación es de M13d. 9 tests (driver + `t.TempDir`): files respeta `.gitignore`, files glob, files hidden+max, fuzzy orden/estabilidad, fuzzy max, grep todos los matches con rangos reconstruibles por `line:sub`, grep glob/case/max, grep paralelo completo (40 ficheros, sin pérdida ni duplicado), y errores (root inexistente→ENOENT, opts malas→EINVAL). Faltan de M13b: http.stream, proc, text-Blocks. Puntero sigue en M13b. |
| 2026-07-06 | **M13b** (ws) | `nu.ws` sobre wasm (§11). `registerWsWasm` cuelga la primitiva `ws._connect` (⏸) —el handshake bloqueante corre en la goroutine de fondo del scheduler y, ya conectado, registra el handle **Ws** con `inst.AllocHandle` (seguro desde el fondo: sólo toca la `handleTable`, mutex-safe por diseño de handle.go; NO la VM)— y los métodos del handle: `send`/`recv` (⏸, enrutados por `__hcall_s`) y `close` (síncrono, `__hcall`). **Reusa el núcleo VM-agnóstico de ws.go** (`rt.dialWs`, `luaWs.send`/`recv`/`close`, `isWsNormalClose`, `errWsClosed`, `classifyTransportError`): no se reimplementó nada de red/protocolo. Sólo se añadió `parseWsOptsWasm` (opts desde `map[string]any`, contrato de §11: url obligatoria, headers string→string, timeout_ms positivo → EINVAL) y **un nil-guard en `luaWs.close`** (`untrackWs` sólo si hay scheduler: el rastreo a nivel de Runtime es de M13d) —sin cambiar el comportamiento gopher; su suite sigue verde—. Wrapper Lua (AddPreludio, estilo vmwasm_re.go) que envuelve el handle de `ws._connect` y le añade send/recv/close al despacho correcto. **Hallazgo/decisión:** `close` **NO libera el handle** (a diferencia de Region:destroy, M11): `Ws:close()` es idempotente por contrato, y liberarlo daría ECLOSED al segundo cierre; `closeOnce` ya cubre la idempotencia y el handle sobrevive como en gopher (muere por GC). 6 tests (driver + servidores locales httptest/coder-websocket): eco send/recv round-trip (varios mensajes en orden), recv→nil al cerrar el servidor (idempotente), send-tras-close→ECLOSED, connect a puerto cerrado→ENET, close idempotente (×3), y validación de opts→EINVAL (4 casos). Faltan de M13b: http.stream, search, proc, text-Blocks. Puntero sigue en M13b. |
| 2026-07-06 | **M13b** (codecs) | Primer módulo del **catálogo real** sobre wasm: los codecs `nu.json`/`nu.toml`/`nu.yaml` (§12). `internal/runtime/vmwasm_codecs.go`: `registerCodecsWasm(p)` cuelga los seis `encode`/`decode` como HostFn de vmwasm, **reusando las mismas librerías Go** que el backend gopher (encoding/json, BurntSushi/toml, yaml.v3) — el trabajo de serialización es idéntico; sólo cambia el marshaling de la frontera, que en wasm ya lo hace el wire (los valores llegan como `any` Go). `wireToEncodable` valida G11 (UTF-8 estricto en strings y claves) + NaN/Inf, traduce el sentinel `Null` y resuelve la ambigüedad []/{} de una tabla vacía a **objeto** (§12); `decodedToWire` mapea `null`→`nu.json.NULL` (sólo JSON) y `json.Number`→integer/float. 10 tests 🔒: round-trip JSON anidado, **integer vs float preservados (mejora de Lua 5.4 sobre gopher)**, **NULL round-trip**, **G11 UTF-8 estricto→EINVAL**, tabla vacía→`{}`, NaN/Inf→EINVAL, **TOML array-de-tablas** (providers.toml), TOML raíz-no-objeto→EINVAL, YAML, JSON inválido→EINVAL. **⚠️ Gotcha de Go documentado (importante):** un fichero `*_wasm.go` recibe la restricción de build implícita `GOARCH=wasm` y Go lo **excluye del build linux sin error** (el binario compila como si no existiera, sus tests no corren pero `go test` dice "ok"). Por eso el catálogo wasm usa el **prefijo** `vmwasm_<módulo>.go`, nunca el sufijo `_wasm`. `internal/runtime` pasa a importar `internal/vmwasm`. Faltan de M13b: fs/proc/sys/http/ws/search/text/re/log. Puntero sigue en M13b. |
| 2026-07-06 | **M13a** | Loader curado sobre wasm (DM5, api.md §14) — el **mecanismo** del loader (primer sub-hito de M13; ver la nota de descomposición en el puntero). `loader.go`: `Pool.RegisterModule` (unicidad de nombre = identidad del plugin: un duplicado da EEXIST), `registerLoader` (primitiva `nu.loader._source` en todo Pool, también en workers), y `TopoOrder` (orden de init por dependencias con detección de ciclos y deps ausentes). `host.go` (`preludioLoader`): `require` propio curado —la lib `package` de PUC NO se abre; el baseline sí trae `load`— con caché de una sola carga, detección de ciclos (EINVAL, no cuelga), y `__loader_reload` (reload best-effort G2). Presente en el estado principal y en los workers (comparten los módulos del padre, api.md §13). 11 tests 🔒 (verde con `-race`): require básico/**cachea (una sola ejecución, misma identidad)**/dependencias transitivas, módulo ausente→ENOENT, **ciclo→EINVAL**, **unicidad→EEXIST**, **reload re-ejecuta (G2)**, TopoOrder ordena/detecta-ciclo/detecta-dep-ausente, y **require dentro de un worker**. `TestBaselineLibs` actualizado: `require` pasa de nil a función (curada); `package` sigue nil (DM5). **Falta de M13 (M13b-d):** cablear el catálogo real de primitivas, el compositor real y las 8 extensiones, y pasar la suite con `NU_VM=wasm` — la integración con el Runtime. Puntero → M13b. |
| 2026-07-06 | **M12** | Workers como instancias wasm (ADR-008/§13). El regalo de ADR-019: cada worker es una **Instance wasm con memoria lineal físicamente aislada** (más fuerte que el `*LState` de gopher, que comparte el heap de Go). `worker.go`: `workerChannels` (dos colas acotadas `cap=16` + `done`), el `worker` (Instance + canales + join), y las primitivas del lado padre `worker._spawn` (sync: crea la Instance del worker con registro filtrado por caps y la arranca en su goroutine con su propio `RunTasks`), `worker._send`/`worker._recv` (⏸, backpressure y fin-de-canal), `worker._terminate`. `parseWorkerCaps` + `workerGrants` implementan G6: módulo entero (`"fs"`) vs función exacta (`"fs.read"`), deny-by-default, `capsGiven` distingue sin-caps (toda la API) de `caps={}`. `host.go`: `preludioWorkerHost` (nu.worker.spawn → Worker con send/recv/on_message/terminate; la **exclusión recv/on_message G8** en campos del Worker, serializada por el estado principal single-thread — las tres reglas lanzan EINVAL en el acto), `preludioWorkerParent` (nu.worker.parent.send/recv, sin spawn: no hay anidamiento), `preludioWorkerCommon` (`__check_msg`: rechaza function/thread/handle antes de ceder). El preludio se ramifica por `Pool.isWorker` (worker: sin nu.ui, sin nu.events, sin spawn). **Hallazgo clave (wasm):** cerrar el módulo NO interrumpe un Call en vuelo; la interrupción de un **bucle de CPU puro** en `worker:terminate` (sin watchdog, G15) exige `wazero.RuntimeConfig.WithCloseOnContextDone(true)` + un ctx cancelable por Instance (el trampolín Snapshot/Restore sigue funcionando con ese config) — también es la base del watchdog por época (DM4). 13 tests 🔒 (verde con `-race`): round-trip, **aislamiento físico de memoria**, **backpressure** (acotado y bloqueado, determinista), mensaje copiado, send no-serializable→EINVAL, **caps G6 (4 casos)**, **G8 (a) y (c)**, on_message entrega en orden, **sin watchdog** (quema 2e6 iteraciones), **terminate interrumpe un bucle de CPU puro**, terminate no afecta al padre, recv-tras-terminate→nil. `Pool.StopWorkers` para el apagado ordenado. **Alcance (como M09-M11):** `module` es código fuente; su resolución por el loader (`require`) es M13. Desviación anotada: nu.task es intrínseco al runtime del worker (preludio), siempre presente; las caps filtran los módulos respaldados por primitivas. Puntero → M13. |
| 2026-07-06 | **M11** | Binding de UI sobre la frontera wasm (C2/C5). `ui.go`: interfaz `UIBackend` (abstrae el compositor —VM-agnóstico según el censo M01—), `Pool.SetUIBackend`, y el catálogo `nu.ui.*` como host functions: `size`/`caps`/`region`/`block`/`clipboard_set`/`clipboard_get` (⏸). **Region** es un handle (C5, sobre M10) con 11 métodos (blit/fill/clear/move/resize/raise/lower/show/hide/destroy/cursor) despachados al backend; `blit` resuelve el handle del **Block** en Go (round-trip C5). `destroy` libera su propio handle (nuevo `inst.dispatchHandle` que el despacho síncrono expone) → reusar la Region da `ECLOSED` (api.md §6). **Block** es un handle con `.width`/`.height`. `preludioInput`: la **pila de input** y la **resolución de secuencias** de teclas viven en el preludio (los handlers son funciones Lua, como el bus de eventos M08) — `on_input` (consume/deja-pasar), `keymap` (acorde + secuencia, consume por defecto, `false` explícito **cede**), `__ui_dispatch_input` (arriba→abajo), `__ui_timeout` (resetea el prefijo), y `nu.has("ui")` (headless G20: sin backend, `nu.ui` no existe). Go inyecta eventos con `FeedInput` (la puerta que el driver real cablea en M13). 15 tests 🔒: headless, size/caps, métodos e identidad de Region, **destroy→ECLOSED**, dims de Block, **blit resuelve el Block**, cursor último-gana, pila consume/pop, keymap acorde/**cede**/**secuencia**/**timeout**, y `FeedInput`. **Decisión anotada (M11):** en gopher los callbacks de input se guardan como `*lua.LFunction`; en wasm eso es imposible (Go no sostiene refs a funciones Lua), así que la pila y el resolver viven en el preludio (sigue siendo "core": el preludio se compila en el binario). El resolver usa progreso por-handler (no el buffer global de input.go); prueba consumo/cesión/secuencia/timeout, y la paridad fina (re-inyección del prefijo abortado) se cierra al cablear el driver real en M13. **Alcance (como M09/M10):** se entrega el mecanismo contra `UIBackend` y una stub de grabación; el compositor real y el arranque de la pantalla desnuda + el chat son la integración con el Runtime (M13). Puntero → M12. |
| 2026-07-06 | **M10** | Userdata como handles opacos (C5). `handle.go`: `handleTable` por Instance (mutex-safe), `AllocHandle`/`GetHandle`/`FreeHandle`, `HandleMethod` y `RegisterHandleMethod`; despacho por dos host functions genéricas (`__handle_call` síncrona, `__handle_call_s` ⏸) que `registerHandleDispatch` instala en cada `NewPool`. `host.go`: en el preludio, un handle cruza el wire como su índice (tag `W_HANDLE`) y en Lua es una tabla `{__id}` con metatable `__handle_mt` cuyo `__index` despacha `h:metodo(...)` al global `__hcall`; ciclo de vida "quien crea, mata" (§6) con `ECLOSED` accionable al reusar un handle liberado. 4 tests 🔒: métodos con estado, identidad opaca independiente, **liberado→ECLOSED**, y round-trip por una primitiva sin perder identidad. **Arreglo de robustez (anotado):** el decoder del wire (`wire.go`) acotaba `make([]any, n)` con un `n` sin validar; bytes corruptos (p. ej. la costura `SetDispatcher` de M02 pasando bytes crudos a `Decode`) pedían ~1e9 elementos → OOM. Nueva guardia `decoder.count()`: rechaza cualquier recuento mayor que los bytes restantes (cada valor cuesta ≥1 byte, así que jamás hay falso positivo). `TestDispatcherCostura` actualizado para probar el rechazo con un id **fuera de rango** (el default ya no es "rechaza todo": id 0/1 son las primitivas de despacho de handles). Runtime wazero **compartido a nivel de proceso** (`sync.Once`): el blob se compila con el JIT una vez, no por Pool — evita el OOM de N runtimes JIT en la suite. **Alcance (igual que M09):** M10 entrega el **mecanismo** de handles y lo prueba con un tipo de ejemplo (`Counter`); los **tipos concretos** del catálogo (Proc/Stream/Ws/Watcher/Timer/Future) se registran contra las implementaciones Go del kernel en la **integración con el Runtime** (M13), donde `RegisterHandleMethod` envuelve cada método real. Suite completa verde; subconjunto de concurrencia con `-race`. Puntero → M11. |
| 2026-07-03 | **M09** | El **mecanismo** de primitivas host, síncronas y **suspendentes**. `Pool.Register` (síncrona: thunk de dispatch directo, M05) y `Pool.RegisterSuspending` (⏸: el thunk **cede** al scheduler con `op="hostcall"`; el driver Go corre el HostFn en una goroutine de fondo —sin tocar la VM, contrato documentado— y reanuda con los valores o el error estructurado). `scheduler.go`: `performHostcall` + `errToMap`. 4 tests 🔒 (verde con `-race`): primitiva síncrona, **`nu.fs.read` suspendente real** (lee un fichero de disco), **la clave — una primitiva ⏸ que tarda 50 ms cede al scheduler y otra task avanza mientras** (el último eslabón del modelo async con IO real), y error estructurado de primitiva ⏸ capturable por `pcall`. **Alcance:** el mecanismo (ambos tipos) está probado; **registrar el catálogo completo** (fs/proc/http/ws/search/text/codecs) contra las implementaciones Go del kernel es volumen mecánico que se hace en la integración con el `Runtime` (M13). Puntero → M10. |
| 2026-07-03 | **M08** | Bus de eventos `nu.events` (§4, G10) + timers `nu.task.every`, todo síncrono en Lua (`preludioEvents`). `on`/`once`/`emit` con la semántica de G10: **foto** de suscriptores al emitir, **cancelar surte efecto inmediato**, subs añadidos durante el despacho solo ven eventos futuros, emits anidados **encolados por anchura** (no recursión; guardia anti-ping-pong), cada handler bajo `pcall` (ADR-008). 8 tests 🔒: básico, once, cancel, **cancel-durante-despacho**, **sub-durante-despacho**, **anidado-por-anchura**, handler-aislado, y `every` (timer periódico que se para). **Pendiente:** el input de UI va con M11 (acoplado al compositor). Puntero → M09. |
| 2026-07-03 | **M07** | Cancelación cooperativa + superficie `nu.task` completa, toda sobre el bucle de M06. `host.go`: `__resume` gana `__current` (para cleanup), `__finish` (cleanups LIFO + notifica awaiters), y manejo de futures y cancelación; `preludioTask` con **future** (rendez-vous), **all** (alineado con inputs, G27), **race**, **cleanup** (LIFO), **cancel** (cooperativo, §1.3: no se reanuda, corre cleanups, `ECANCELED` observable) y **defer**. 6 tests 🔒 (verde con `-race`): future, all-alineado (G27), race, cleanup-LIFO, **cancel corre cleanups**, y cancel observable (`ECANCELED` no capturable por el pcall interno). **Pendiente arrastrado:** el **watchdog por época de wazero** (DM4, preempción de bucles de CPU) — su presupuesto de slice vive en el Runtime, así que se cablea en la integración (M09+). Puntero → M08. |
| 2026-07-03 | **M06** | **El corazón arquitectónico**: [ADR-020](adr.md#adr-020--el-puente--definitivo-tasks-como-corrutinas-lua-nativas-reemplaza-adr-011-en-la-conmutación) (reemplaza a ADR-011 en la conmutación) + el scheduler por **corrutinas nativas**. Shim: export `nu_sched_step` (puente Go↔bucle Lua). `host.go`: el scheduler Lua (tasks como `coroutine`, ⏸ como `coroutine.yield` de una petición, `__sched_step` como el paso). `scheduler.go`: el driver Go `RunTasks` (event loop de ADR-004 sin token: recoge peticiones cedidas, las cumple en goroutines de fondo, reanuda). 6 tests 🔒 (verde con `-race`): task simple, **concurrencia real** (dos tasks intercaladas `B1,B2,A1,A2` por yield nativo), await (incl. task ya terminada), **yield a través de pcall dentro de una task** (G31 imposible en gopher, aquí natural), y cancelación por contexto (base del apagado M07). **Alcance:** el núcleo del modelo (spawn/sleep/await/loop/cancel) está probado end-to-end; el resto de la superficie `nu.task` (all[G27]/race/every/defer/future/cleanup) **extiende el mismo bucle** y se completa junto a M07 (cancel/cleanup lo necesitan) — anotado en el puntero. Puntero → M07. |
| 2026-07-03 | **M05** | Marshaling (C1) + registro de host functions (C2): la **keystone**. `wire.go`: codec TLV byte-seguro (G11: strings crudos, sin re-codificar) con anchuras fijas u32 compatibles con `string.pack`/`unpack` de Lua 5.4; tags para nil/bool/int/float/string/array/map/**handle** (C5, para M10)/**NULL** (sentinel de G11). `host.go`: `hostRegistry` (nombre→id→`HostFn`) en el `Pool`; `Pool.Register` (la vía de M09+); `dispatchPrimitive` con protocolo de estado (byte 0=éxito/1=error); `StructuredError` que cruza `{code,message,detail}` fielmente (C4, paridad §1.4); y el **preludio Lua** (codec espejo + monta la tabla `nu` de thunks sobre `__nu_host`). El preludio se ejecuta al crear cada instancia. 9 tests 🔒: round-trip Go y Go↔Lua↔Go real (echo), **G11 bytes no-UTF-8 intactos en ambos sentidos**, integer vs float (5.4), tablas anidadas, sentinel NULL distinto de nil, y **cruce de errores estructurados y genéricos** capturables por `pcall`. Suite verde con `-race`. Con esto M09 (las primitivas) es mecánico. Puntero → M06. |
